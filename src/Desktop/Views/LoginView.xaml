<UserControl x:Class="Noteapp.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Noteapp.Desktop.Views"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!--Row 0-->
        <Label Grid.Row="0" Grid.Column="0" Content="Email" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Email}" />

        <!--Row 1-->
        <Label Grid.Row="1" Grid.Column="0" Content="Password" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Password}" />

        <!--Row 2-->
        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Command="{Binding LoginCommand}" Content="Log in" />
        
        <!--Row 3-->
        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Command="{Binding LogoutCommand}" Content="Log out" />
        
    </Grid>
</UserControl>
