<UserControl x:Class="Noteapp.Desktop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Noteapp.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0">Email</Label>
        <Label Grid.Row="0" Grid.Column="1"  Content="{Binding Email}" />

        <Button Grid.Row="1" Grid.ColumnSpan="2" Content="Log out" Command="{Binding LogoutCommand}" />

        <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Delete account" />


    </Grid>
</UserControl>
