<UserControl x:Class="Noteapp.Desktop.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Noteapp.Desktop.Views"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!--Row 0-->
        <TextBox Grid.Row="0" Text="{Binding Email}" x:Name="EmailTextBox" 
                 VerticalAlignment="Center" HorizontalAlignment="Center" Width="400"/>
        <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" 
                   IsHitTestVisible="False" Text="Email" Margin="0,0,300,0" Foreground="DarkGray" FontSize="16">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=EmailTextBox}" Value="">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!--Row 1-->
        <TextBox Grid.Row="1" Text="{Binding Password}" x:Name="PasswordTextBox"
                 VerticalAlignment="Center" HorizontalAlignment="Center" Width="400"/>
        <TextBlock Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" 
                   IsHitTestVisible="False" Text="Password" Margin="0,0,300,0" Foreground="DarkGray" FontSize="16">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=PasswordTextBox}" Value="">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!--Row 2-->
        <Button Grid.Row="2" Command="{Binding RegisterCommand}" Content="Register" />
        
    </Grid>
</UserControl>
