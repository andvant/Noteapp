<UserControl x:Class="Noteapp.Desktop.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Noteapp.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <!--Row 0-->
        <Label Grid.Row="0" Grid.Column="0" Content="Email" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Email}" />

        <!--Row 1-->
        <Label Grid.Row="1" Grid.Column="0" Content="Password" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Password}" />

        <!--Row 2-->
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Output}" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" FontSize="14"/>

        <!--Row 3-->
        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Command="{Binding RegisterCommand}" Content="Register" />
        
    </Grid>
</UserControl>
